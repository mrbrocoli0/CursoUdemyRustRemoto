use std::collections::HashSet;
use std::prelude::v1::*;

fn main(){

    let st: MiEst;
    let algo: HashSet<i32>;
    let mapa: std::collections::HashMap<i32, i32>; //Ruta completa de dónde viene
    //Mapa de 2 puntos
    
    //Prelude = preludio
    let opcion: Option<i32>;
    let opcion2: std::prelude::v1::Option<i32>;
}
struct MiEst{
}
